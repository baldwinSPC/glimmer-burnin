# Review assignment.
#
# One maintainer today, so this is a catch-all rather than a partition. It exists
# so review is assigned automatically rather than depending on someone noticing,
# and so the paths below can be split as that changes.
*                       @baldwinSPC

# The pieces where a mistake is hardest to reverse, listed separately so they
# are visible as a category even while they resolve to the same person:
#
#   published tags are immutable and a runner tag is executed by a node's
#   readiness gate, so a bad one degrades a whole fleet at once
/runners/               @baldwinSPC
/.github/workflows/     @baldwinSPC
#   the wire contract other people's tooling parses
/pkg/contract/          @baldwinSPC
/api/                   @baldwinSPC
#   the licence policy, which is what keeps this project publishable
/hack/licenses-allowed.txt @baldwinSPC
