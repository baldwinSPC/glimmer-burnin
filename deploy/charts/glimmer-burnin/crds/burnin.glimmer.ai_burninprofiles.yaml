---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: burninprofiles.burnin.glimmer.ai
spec:
  group: burnin.glimmer.ai
  names:
    categories:
    - burnin
    kind: BurnInProfile
    listKind: BurnInProfileList
    plural: burninprofiles
    shortNames:
    - bip
    singular: burninprofile
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.tests[*]
      name: Tests
      type: integer
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: BurnInProfile is an ordered acceptance suite of BurnInTests.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: |-
              BurnInProfileSpec is an ordered acceptance suite: which tests, in what order,
              and the overall verdict policy. A profile is what an operator "runs" against
              hardware ("acceptance", "quick-smoke", "pair-network").
            properties:
              failFast:
                description: |-
                  FailFast stops the run at the first failing test (default false — a full
                  acceptance sweep usually wants every result even after one failure).
                type: boolean
              sinks:
                description: Sinks names BurnInSinks (same namespace) that results
                  are exported to.
                items:
                  type: string
                type: array
              tests:
                description: |-
                  Tests are executed in order. Each references a BurnInTest by name (same
                  namespace) or inlines a spec.
                items:
                  description: ProfileTest references a BurnInTest or inlines one.
                  properties:
                    inline:
                      description: Inline defines the test directly (mutually exclusive
                        with TestRef).
                      properties:
                        checkpointIntervalSeconds:
                          description: |-
                            CheckpointIntervalSeconds is how often a long execution publishes its
                            metrics so far onto the TestResult. 0 or nil disables checkpointing.

                            A multi-hour thermal soak that is cancelled, evicted, or lost to a node
                            reboot at minute 200 otherwise reports nothing at all: the runner's
                            metrics only reach the status when the pod terminates. Checkpointing
                            keeps the evidence gathered so far, so an interrupted soak still tells
                            you what the temperatures and clocks were doing.

                            A checkpoint is EVIDENCE, never a verdict. Thresholds are evaluated once,
                            against the final metrics of a completed execution — a mid-run sample
                            that dips below a threshold is not a failure, because the run is not
                            over. Checkpoints overwrite in place rather than accumulating, so a long
                            soak cannot grow its status object without bound.
                          format: int32
                          minimum: 0
                          type: integer
                        durationSeconds:
                          description: |-
                            DurationSeconds bounds a single execution (0 = the kind's default).

                            At Group scope it also buys margin for the RENDEZVOUS. Every rank's pod
                            deadline is this plus a fixed grace, and a rank spends part of that window
                            waiting for the rest of the cohort to be scheduled and to pull the runner
                            image — so on a cold image cache a large group can lose real test time to
                            the wait. Raising this, or pre-pulling the runner image onto the target
                            nodes, are the two things that help; see issue #122 and the note in
                            internal/controller/group.go.
                          format: int32
                          minimum: 0
                          type: integer
                        hostNetwork:
                          description: |-
                            HostNetwork runs the test pod on the host network (needed for direct host
                            RDMA in some fabrics). Defaults false.
                          type: boolean
                        kind:
                          description: Kind selects built-in runner defaults + result
                            parsing.
                          type: string
                        repeatCount:
                          default: 1
                          description: |-
                            RepeatCount is how many times this test executes sequentially on each
                            node. EVERY execution must pass for the test to pass — the repeats are
                            an AND, not a best-of.

                            This is the intermittent-fault gate. The faults that matter most in
                            burn-in are the ones that do not reproduce on the first try: a marginal
                            solder joint, a link that retrains under thermal cycling, a DIMM that
                            miscompares once an hour. A single clean pass does not distinguish a
                            good part from a part that fails one run in five, and running the test
                            N times is what makes that distinction. Repeats are sequential, never
                            parallel: the point is to cycle the hardware, and two concurrent copies
                            on one node would contend for the very resource under test.

                            Do not confuse this with RetryOnErrorLimit on the run. A repeat re-runs
                            a test that already produced a verdict and makes the verdict stricter; a
                            retry re-runs a test that produced no verdict at all. A repeat can turn
                            a Passed into a Failed, and must never turn a Failed into a Passed.
                          format: int32
                          minimum: 1
                          type: integer
                        resources:
                          description: Resources requested per test pod (e.g. nvidia.com/gpu,
                            rdma/rdma_shared_device_a).
                          properties:
                            claims:
                              description: |-
                                Claims lists the names of resources, defined in spec.resourceClaims,
                                that are used by this container.

                                This field depends on the
                                DynamicResourceAllocation feature gate.

                                This field is immutable. It can only be set for containers.
                              items:
                                description: ResourceClaim references one entry in
                                  PodSpec.ResourceClaims.
                                properties:
                                  name:
                                    description: |-
                                      Name must match the name of one entry in pod.spec.resourceClaims of
                                      the Pod where this field is used. It makes that resource available
                                      inside a container.
                                    type: string
                                  request:
                                    description: |-
                                      Request is the name chosen for a request in the referenced claim.
                                      If empty, everything from the claim is made available, otherwise
                                      only the result of this request.
                                    type: string
                                required:
                                - name
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - name
                              x-kubernetes-list-type: map
                            limits:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Limits describes the maximum amount of compute resources allowed.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                            requests:
                              additionalProperties:
                                anyOf:
                                - type: integer
                                - type: string
                                pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                x-kubernetes-int-or-string: true
                              description: |-
                                Requests describes the minimum amount of compute resources required.
                                If Requests is omitted for a container, it defaults to Limits if that is explicitly specified,
                                otherwise to an implementation-defined value. Requests cannot exceed Limits.
                                More info: https://kubernetes.io/docs/concepts/configuration/manage-resources-containers/
                              type: object
                          type: object
                        runner:
                          description: |-
                            Runner overrides the image/command for this test. When empty the operator
                            uses the built-in image for Kind. This is the vendor/heterogeneity seam:
                            a new accelerator or NIC ships a runner image, not a controller change.
                          properties:
                            args:
                              items:
                                type: string
                              type: array
                            capabilities:
                              description: |-
                                Capabilities are Linux capabilities ADDED to the runner container,
                                narrower than Privileged for a capability need that does not also
                                require a device-cgroup grant.

                                IT DOES NOT, BY ITSELF, MAKE /dev/kmsg READABLE IN-CLUSTER, even
                                combined with a HostPaths CharDevice mount and RunAsUser: 0 — see
                                CapabilitySYSLOG's own doc comment and #302. This field exists because
                                #134's truth table names CAP_SYSLOG as one of three grants /dev/kmsg
                                needs at once, and until this field existed there was no way to request
                                it without asking for Privileged: true's much larger blast radius too —
                                but #302 then measured that the operator's own device-cgroup grant is
                                missing regardless, so requesting CAP_SYSLOG here still leaves
                                /dev/kmsg at EPERM. host-health's kmsg recipe still needs
                                Privileged: true in-cluster today; this field is for whatever capability
                                need arrives next that is not gated on a missing device-cgroup rule.

                                Add-only, deliberately: a Drop list is a hardening feature and a
                                different conversation from the narrower-grant one this field answers.

                                Enumerated rather than a free string, matching ImagesByVendor's own
                                reasoning (#274/#257): a typo'd capability name is accepted by the
                                apiserver either way, but an enum makes it a validation error instead of
                                a silently ineffective grant that looks like broken hardware. Widen the
                                enum the moment a runner legitimately needs another one.

                                ONLY MEANINGFUL WITH RunAsUser: 0. A capability added to the bounding
                                set does nothing for a non-root uid without ambient capabilities — the
                                same measurement that showed Privileged alone fails for this image's
                                default uid 65532. Setting Capabilities without RunAsUser: 0 is refused
                                at plan time (buildPlan) rather than producing a probe that silently
                                reads nothing and reads as broken hardware.
                              items:
                                description: |-
                                  Capability is a Linux capability RunnerSpec.Capabilities may add.

                                  Enumerated rather than a free string (#302), matching VendorImage's own
                                  reasoning: a typo'd capability name is accepted by the apiserver either
                                  way, but an enum turns it into a validation error instead of a silently
                                  ineffective grant that looks like broken hardware at run time. Widen this
                                  the moment a runner legitimately needs another one.

                                  CapabilitySYSLOG does NOT make /dev/kmsg readable in-cluster by itself —
                                  see its own doc comment.
                                enum:
                                - SYSLOG
                                type: string
                              maxItems: 4
                              type: array
                              x-kubernetes-list-type: set
                            command:
                              items:
                                type: string
                              type: array
                            env:
                              items:
                                description: EnvVar represents an environment variable
                                  present in a Container.
                                properties:
                                  name:
                                    description: |-
                                      Name of the environment variable.
                                      May consist of any printable ASCII characters except '='.
                                    type: string
                                  value:
                                    description: |-
                                      Variable references $(VAR_NAME) are expanded
                                      using the previously defined environment variables in the container and
                                      any service environment variables. If a variable cannot be resolved,
                                      the reference in the input string will be unchanged. Double $$ are reduced
                                      to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                      "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                      Escaped references will never be expanded, regardless of whether the variable
                                      exists or not.
                                      Defaults to "".
                                    type: string
                                  valueFrom:
                                    description: Source for the environment variable's
                                      value. Cannot be used if value is not empty.
                                    properties:
                                      configMapKeyRef:
                                        description: Selects a key of a ConfigMap.
                                        properties:
                                          key:
                                            description: The key to select.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the ConfigMap
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fieldRef:
                                        description: |-
                                          Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                          spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                        properties:
                                          apiVersion:
                                            description: Version of the schema the
                                              FieldPath is written in terms of, defaults
                                              to "v1".
                                            type: string
                                          fieldPath:
                                            description: Path of the field to select
                                              in the specified API version.
                                            type: string
                                        required:
                                        - fieldPath
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      fileKeyRef:
                                        description: |-
                                          FileKeyRef selects a key of the env file.
                                          Requires the EnvFiles feature gate to be enabled.
                                        properties:
                                          key:
                                            description: |-
                                              The key within the env file. An invalid key will prevent the pod from starting.
                                              The keys defined within a source may consist of any printable ASCII characters except '='.
                                              During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                            type: string
                                          optional:
                                            default: false
                                            description: |-
                                              Specify whether the file or its key must be defined. If the file or key
                                              does not exist, then the env var is not published.
                                              If optional is set to true and the specified key does not exist,
                                              the environment variable will not be set in the Pod's containers.

                                              If optional is set to false and the specified key does not exist,
                                              an error will be returned during Pod creation.
                                            type: boolean
                                          path:
                                            description: |-
                                              The path within the volume from which to select the file.
                                              Must be relative and may not contain the '..' path or start with '..'.
                                            type: string
                                          volumeName:
                                            description: The name of the volume mount
                                              containing the env file.
                                            type: string
                                        required:
                                        - key
                                        - path
                                        - volumeName
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      resourceFieldRef:
                                        description: |-
                                          Selects a resource of the container: only resources limits and requests
                                          (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                        properties:
                                          containerName:
                                            description: 'Container name: required
                                              for volumes, optional for env vars'
                                            type: string
                                          divisor:
                                            anyOf:
                                            - type: integer
                                            - type: string
                                            description: Specifies the output format
                                              of the exposed resources, defaults to
                                              "1"
                                            pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                            x-kubernetes-int-or-string: true
                                          resource:
                                            description: 'Required: resource to select'
                                            type: string
                                        required:
                                        - resource
                                        type: object
                                        x-kubernetes-map-type: atomic
                                      secretKeyRef:
                                        description: Selects a key of a secret in
                                          the pod's namespace
                                        properties:
                                          key:
                                            description: The key of the secret to
                                              select from.  Must be a valid secret
                                              key.
                                            type: string
                                          name:
                                            default: ""
                                            description: |-
                                              Name of the referent.
                                              This field is effectively required, but due to backwards compatibility is
                                              allowed to be empty. Instances of this type with an empty value here are
                                              almost certainly wrong.
                                              More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                            type: string
                                          optional:
                                            description: Specify whether the Secret
                                              or its key must be defined
                                            type: boolean
                                        required:
                                        - key
                                        type: object
                                        x-kubernetes-map-type: atomic
                                    type: object
                                required:
                                - name
                                type: object
                              type: array
                            hostPaths:
                              description: |-
                                HostPaths are paths on the NODE made visible inside the runner container.

                                This is the most security-relevant field in this API and it is deliberately
                                the most explicit one. Every entry hands the runner a piece of the host's
                                own filesystem; it is not made safe by the pod being short-lived, and a
                                writable mount of a path that only needed reading is a real escalation.
                                Nothing is mounted unless it is named here — there is no implicit host
                                access anywhere in this operator.

                                A burn-in runner legitimately needs it, which is why the field exists
                                rather than being refused. Hardware acceptance measures the host, and the
                                two facts below were measured on a real cluster, not inferred:

                                  - RDMA fabric tests are IMPOSSIBLE without it. `privileged: true` plus
                                    `hostNetwork: true` does NOT give a pod the host's user-verbs device
                                    nodes: /dev/infiniband inside such a pod holds only rdma_cm and umad*,
                                    while the host also has uverbs*. uverbs is what ibv_create_cq opens, so
                                    ib_write_bw dies with "Couldn't create CQ / Failed to create CQs" and
                                    the nccl runner fails the same way. Mounting the host's
                                    /dev/infiniband is what makes uverbs* appear in the pod.
                                  - Xid scanning needs /dev/kmsg, which is not in a container's default
                                    /dev. Without it host-health reports xid_source=none and OMITS
                                    xidEvents, so a gate on that metric fails closed and certifies nothing.

                                The shape is deliberately narrow rather than a general
                                []corev1.Volume + []corev1.VolumeMount. Every mount a burn-in runner has
                                ever needed is a host device or a host log path, and a curated field states
                                that intent where a general volume list would bury it: a reviewer can read
                                what a BurnInTest takes from the node, and from which direction, without
                                decoding a volume source. It also lets this API refuse what the general
                                form cannot — Type here admits only the assert-only hostPath types, so a
                                BurnInTest can never ask the kubelet to CREATE a path on a node.
                              items:
                                description: |-
                                  HostPathMount makes ONE path on the node visible inside the runner container.

                                  It is honoured identically at Node scope and at Pair scope, on BOTH pods of a
                                  pair: a fabric test that could reach the verbs devices from only one end of
                                  the link would measure nothing.

                                  See RunnerSpec.HostPaths for what this costs and why a burn-in runner needs
                                  it anyway. The short version: this is host access, it is granted only where
                                  named, and it is granted read-only unless the entry says otherwise.
                                properties:
                                  mountPath:
                                    description: |-
                                      MountPath is where that path appears INSIDE the runner container.

                                      It is required rather than defaulted to Path, because a mount whose two
                                      ends are written out is a mount a reviewer can check. It is also this
                                      list's identity: two entries may not share a MountPath.
                                    minLength: 1
                                    pattern: ^/
                                    type: string
                                  path:
                                    description: Path is the absolute path ON THE
                                      HOST, e.g. "/dev/infiniband".
                                    minLength: 1
                                    pattern: ^/
                                    type: string
                                  readOnly:
                                    default: true
                                    description: |-
                                      ReadOnly mounts the path without write access. It DEFAULTS TO TRUE, which
                                      is the direction a security-relevant default has to fall: an author who
                                      needs the runner to write to the host has to say so, and an author who
                                      forgets gets the harmless mount rather than the dangerous one.

                                      It is a pointer precisely so that "unset" is distinguishable from "false".
                                      A plain bool would make a Go-constructed object — or one written before
                                      this field existed — silently take the writable form.

                                      Which way round the real cases fall:

                                        - /dev/kmsg is READ-ONLY. host-health only ever reads the ring buffer,
                                          and a burn-in runner has no business writing to the kernel log.
                                        - /dev/infiniband must be WRITABLE (readOnly: false). The user-verbs
                                          device nodes are opened read-write by ibv_open_device, so a read-only
                                          mount fails in the same place, and with much the same message, as no
                                          mount at all.
                                    type: boolean
                                  type:
                                    description: |-
                                      Type asserts what the path must already BE on the node, and the kubelet
                                      refuses the pod if it is anything else.

                                      The permitted values are the assert-only ones. corev1's DirectoryOrCreate
                                      and FileOrCreate are deliberately NOT accepted: a burn-in run touches every
                                      node in a fleet, and an operator that creates paths on hosts to satisfy its
                                      own mounts would mutate the fleet it was sent to measure. Leaving Type
                                      unset keeps Kubernetes' default of no check at all, which also lets the
                                      container runtime create a missing path — so set it whenever you know what
                                      the path is.

                                      Setting it converts "the runner saw an empty directory and reported
                                      nothing" into a pod that refuses to start, which the run records as an
                                      infrastructure Error. That is the better failure: an Error says the
                                      hardware was never judged, where an empty mount looks like a measurement.
                                    enum:
                                    - Directory
                                    - File
                                    - Socket
                                    - CharDevice
                                    - BlockDevice
                                    type: string
                                required:
                                - mountPath
                                - path
                                type: object
                              type: array
                              x-kubernetes-list-map-keys:
                              - mountPath
                              x-kubernetes-list-type: map
                            image:
                              description: |-
                                Image overrides the built-in image for the test's Kind. Empty means "use
                                the default for this Kind", and a Kind with no default refuses at plan
                                time asking for this field rather than pull-failing per node.

                                It is optional because RunnerSpec is no longer only an image override: a
                                test that needs nothing but a readinessProbe or a hostPath would otherwise
                                be forced to pin an image it did not want to change, and pinning one to
                                get a mount is how a fleet ends up on a stale runner tag by accident.
                              type: string
                            imagesByVendor:
                              description: "ImagesByVendor selects the image by the
                                ACCELERATOR VENDOR of the node the\ntest lands on,
                                so one profile can serve a mixed fleet.\n\n\trunner:\n\t
                                \ imagesByVendor:\n\t    - vendor: nvidia\n\t      image:
                                ghcr.io/.../glimmer-burnin-memory-bw:v1\n\t    - vendor:
                                amd\n\t      image: ghcr.io/.../glimmer-burnin-memory-bw-rocm:v1\n\nThis
                                is the project's vendor-neutrality rule expressed
                                in the API rather\nthan worked around. Heterogeneity
                                is DATA — fingerprints and runner images\n— and never
                                a controller branch, and the reconciler still holds
                                that line:\nresolution is a lookup, not an `if amd
                                {}`. What changes is that the data\nfinally has somewhere
                                to go. Before this, `memory-bw` meant nvbandwidth\nfull
                                stop, and a mixed fleet needed either N profiles that
                                drift apart or\nan explicit image that gives up on
                                one profile serving everything.\n\nResolution order
                                is explicit > byVendor > the kind's built-in default.
                                A\nnode whose vendor has neither is a PLAN-TIME error
                                naming the vendor and\nthis field — never a skip,
                                because a node silently not being tested is how\na
                                fleet gets certified without being measured.\n\nA
                                LIST OF PAIRS, not a map keyed by vendor, and the
                                reason is worth\nrecording: Kubernetes cannot validate
                                map KEYS. A structural schema\ndescribes `additionalProperties`
                                — the values — and nothing else, so a\ntypo'd `nvidai:`
                                would be accepted by the apiserver and resolve to
                                no\nimage on every NVIDIA node in the fleet. The only
                                way to check keys is a\nCEL rule, and a CEL rule over
                                a map whose key length the schema cannot\nbound is
                                estimated so expensively that the apiserver REFUSES
                                TO INSTALL\nTHE ENTIRE CRD — measured, not guessed:
                                15.3x over budget even with\nmaxProperties set. As
                                a list, `vendor` is an ordinary enumerated string\nthe
                                apiserver validates for free, and listMapKey rejects
                                duplicates."
                              items:
                                description: VendorImage pins one accelerator vendor's
                                  runner image.
                                properties:
                                  image:
                                    description: Image is the runner image for that
                                      vendor.
                                    minLength: 1
                                    type: string
                                  vendor:
                                    description: |-
                                      Vendor is the accelerator vendor, matching NodeFingerprint's vocabulary.

                                      Enumerated by the apiserver, which is the whole reason this is a list
                                      rather than a map: a typo here is rejected at apply time rather than
                                      resolving to no image on every node of that vendor at 3am.
                                    enum:
                                    - nvidia
                                    - amd
                                    - intel
                                    - tenstorrent
                                    - habana
                                    type: string
                                required:
                                - image
                                - vendor
                                type: object
                              maxItems: 8
                              type: array
                              x-kubernetes-list-map-keys:
                              - vendor
                              x-kubernetes-list-type: map
                            prepare:
                              description: "Prepare seeds a scratch volume from an
                                IMAGE before the runner starts —\nan initContainer,
                                in effect, for the one case a burn-in runner\ngenuinely
                                needs one: a vendor tool the runner image may not\nredistribute
                                (dcgm-diag's DCGM; AMD's RVS would be the same case)
                                but\nthat the SITE is entitled to pull, most often
                                because the identical\nimage is already running as
                                a DaemonSet on every GPU node in the\ncluster (#375).\n\nBefore
                                this field, the only route to that binary was HostPaths
                                — which\nmeans preparing every node in the fleet by
                                hand before the operator can\nmeasure it, exactly
                                the gap dcgm-diag's own README documents as its\nunsatisfying
                                option. An initContainer using the site's own DCGM
                                image\nneeds no host mount and no per-node preparation
                                at all: the vendor\nbinary the runner needs is copied
                                out of a container the site is\nalready entitled to
                                run, immediately before the runner that needs it\nstarts.\n\n\trunner:\n\t
                                \ prepare:\n\t    - image: nvcr.io/nvidia/cloud-native/dcgm:4.5.2-1-ubuntu22.04\n\t
                                \     copyFrom: /usr\n\t      mountPath: /usr/local/dcgm\n\nThis
                                is NOT a general initContainer field, on purpose,
                                for the same\nreason HostPaths is a curated shape
                                rather than []corev1.Volume +\n[]corev1.VolumeMount:
                                every case this field exists for is \"copy a path\nout
                                of an image\", and a curated field states that intent
                                where a\ngeneral initContainer list would let a BurnInTest
                                run arbitrary\npre-runner logic — a much larger surface
                                than seeding a volume.\n\nEach step gets its own emptyDir,
                                populated by copying CopyFrom out of\nImage, then
                                mounted READ-ONLY into the runner container at MountPath
                                —\nthe runner only ever needs to READ a vendor tool
                                it did not build, and\na scratch volume it could write
                                to is a privilege the runner does not\nneed and this
                                field does not grant. The copy happens once, in the\ninitContainer,
                                before the runner container is created at all; the\nrunner
                                never sees Image or CopyFrom, only the result at MountPath."
                              items:
                                description: |-
                                  PrepareStep copies ONE path out of an image into the runner container,
                                  before the runner starts. See RunnerSpec.Prepare for what this is for and
                                  why it is not a general initContainer.
                                properties:
                                  copyFrom:
                                    description: |-
                                      CopyFrom is the path INSIDE Image to copy — recursively, contents and
                                      all — into MountPath.
                                    minLength: 1
                                    pattern: ^/
                                    type: string
                                  image:
                                    description: |-
                                      Image is pulled and run as an initContainer purely to be copied from —
                                      its own entrypoint is never invoked; the step overrides it with a copy
                                      command instead.
                                    minLength: 1
                                    type: string
                                  mountPath:
                                    description: |-
                                      MountPath is where the copied contents appear INSIDE THE RUNNER
                                      container, read-only. This list's identity: two entries may not share
                                      a MountPath, the same rule and the same reason as HostPathMount's.
                                    minLength: 1
                                    pattern: ^/
                                    type: string
                                required:
                                - copyFrom
                                - image
                                - mountPath
                                type: object
                              maxItems: 4
                              type: array
                              x-kubernetes-list-map-keys:
                              - mountPath
                              x-kubernetes-list-type: map
                            priorityClassName:
                              description: |-
                                PriorityClassName points this test's pods at a PriorityClass the SITE
                                manages.

                                Passthrough only. This operator never creates a PriorityClass: a
                                cluster-scoped object minted by a test-runner is a footgun, and it is a
                                permission the operator should not need — its RBAC does not grow for this
                                field, which is the point.

                                It is worth setting on contended hardware, where a multi-day soak is
                                otherwise one preemption away from an Error. Every runner pod already
                                carries the autoscaler's safe-to-evict: false annotation, which handles
                                consolidation; this handles contention, which is a different mechanism
                                and needs a different answer.
                              type: string
                            privileged:
                              description: |-
                                Privileged is sometimes required for RDMA/GPUDirect paths.

                                It is NOT sufficient to reach host device nodes — see HostPaths, which is
                                the field that actually gets them into the container.
                              type: boolean
                            readinessProbe:
                              description: |-
                                ReadinessProbe is what a Pair-scope SERVER runner uses to say it is
                                actually able to accept a connection.

                                It exists because "the container is running" and "the socket is
                                listening" are different claims, and the gap between them is exactly
                                where a fabric test produces its most misleading result: an ib_write_bw
                                or nccl client that connects a moment too early fails with a connection
                                error, and a connection error on a fabric test reads as a bad link. The
                                operator will not start the client pod until the server pod is Ready, so
                                a server that declares a probe here converts that gate from "the kubelet
                                started the process" into "the process answered on its port".

                                Without a probe a pod is Ready as soon as its containers start, which is
                                the weaker guarantee. A server runner whose listener takes any
                                appreciable time to bind should ship one — a tcpSocket probe on the
                                runner's own port is usually enough.

                                It is honoured at every scope; it is only load-bearing at Pair.
                              properties:
                                exec:
                                  description: Exec specifies a command to execute
                                    in the container.
                                  properties:
                                    command:
                                      description: |-
                                        Command is the command line to execute inside the container, the working directory for the
                                        command  is root ('/') in the container's filesystem. The command is simply exec'd, it is
                                        not run inside a shell, so traditional shell instructions ('|', etc) won't work. To use
                                        a shell, you need to explicitly call out to that shell.
                                        Exit status of 0 is treated as live/healthy and non-zero is unhealthy.
                                      items:
                                        type: string
                                      type: array
                                      x-kubernetes-list-type: atomic
                                  type: object
                                failureThreshold:
                                  description: |-
                                    Minimum consecutive failures for the probe to be considered failed after having succeeded.
                                    Defaults to 3. Minimum value is 1.
                                  format: int32
                                  type: integer
                                grpc:
                                  description: GRPC specifies a GRPC HealthCheckRequest.
                                  properties:
                                    port:
                                      description: Port number of the gRPC service.
                                        Number must be in the range 1 to 65535.
                                      format: int32
                                      type: integer
                                    service:
                                      default: ""
                                      description: |-
                                        Service is the name of the service to place in the gRPC HealthCheckRequest
                                        (see https://github.com/grpc/grpc/blob/master/doc/health-checking.md).

                                        If this is not specified, the default behavior is defined by gRPC.
                                      type: string
                                  required:
                                  - port
                                  type: object
                                httpGet:
                                  description: HTTPGet specifies an HTTP GET request
                                    to perform.
                                  properties:
                                    host:
                                      description: |-
                                        Host name to connect to, defaults to the pod IP. You probably want to set
                                        "Host" in httpHeaders instead.
                                      type: string
                                    httpHeaders:
                                      description: Custom headers to set in the request.
                                        HTTP allows repeated headers.
                                      items:
                                        description: HTTPHeader describes a custom
                                          header to be used in HTTP probes
                                        properties:
                                          name:
                                            description: |-
                                              The header field name.
                                              This will be canonicalized upon output, so case-variant names will be understood as the same header.
                                            type: string
                                          value:
                                            description: The header field value
                                            type: string
                                        required:
                                        - name
                                        - value
                                        type: object
                                      type: array
                                      x-kubernetes-list-type: atomic
                                    path:
                                      description: Path to access on the HTTP server.
                                      type: string
                                    port:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: |-
                                        Name or number of the port to access on the container.
                                        Number must be in the range 1 to 65535.
                                        Name must be an IANA_SVC_NAME.
                                      x-kubernetes-int-or-string: true
                                    scheme:
                                      description: |-
                                        Scheme to use for connecting to the host.
                                        Defaults to HTTP.
                                      type: string
                                  required:
                                  - port
                                  type: object
                                initialDelaySeconds:
                                  description: |-
                                    Number of seconds after the container has started before liveness probes are initiated.
                                    More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                                  format: int32
                                  type: integer
                                periodSeconds:
                                  description: |-
                                    How often (in seconds) to perform the probe.
                                    Default to 10 seconds. Minimum value is 1.
                                  format: int32
                                  type: integer
                                successThreshold:
                                  description: |-
                                    Minimum consecutive successes for the probe to be considered successful after having failed.
                                    Defaults to 1. Must be 1 for liveness and startup. Minimum value is 1.
                                  format: int32
                                  type: integer
                                tcpSocket:
                                  description: TCPSocket specifies a connection to
                                    a TCP port.
                                  properties:
                                    host:
                                      description: 'Optional: Host name to connect
                                        to, defaults to the pod IP.'
                                      type: string
                                    port:
                                      anyOf:
                                      - type: integer
                                      - type: string
                                      description: |-
                                        Number or name of the port to access on the container.
                                        Number must be in the range 1 to 65535.
                                        Name must be an IANA_SVC_NAME.
                                      x-kubernetes-int-or-string: true
                                  required:
                                  - port
                                  type: object
                                terminationGracePeriodSeconds:
                                  description: |-
                                    Optional duration in seconds the pod needs to terminate gracefully upon probe failure.
                                    The grace period is the duration in seconds after the processes running in the pod are sent
                                    a termination signal and the time when the processes are forcibly halted with a kill signal.
                                    Set this value longer than the expected cleanup time for your process.
                                    If this value is nil, the pod's terminationGracePeriodSeconds will be used. Otherwise, this
                                    value overrides the value provided by the pod spec.
                                    Value must be non-negative integer. The value zero indicates stop immediately via
                                    the kill signal (no opportunity to shut down).
                                    This is a beta field and requires enabling ProbeTerminationGracePeriod feature gate.
                                    Minimum value is 1. spec.terminationGracePeriodSeconds is used if unset.
                                  format: int64
                                  type: integer
                                timeoutSeconds:
                                  description: |-
                                    Number of seconds after which the probe times out.
                                    Defaults to 1 second. Minimum value is 1.
                                    More info: https://kubernetes.io/docs/concepts/workloads/pods/pod-lifecycle#container-probes
                                  format: int32
                                  type: integer
                              type: object
                            runAsUser:
                              description: |-
                                RunAsUser overrides the uid the runner container runs as. Unset uses the
                                image's own USER, which for every runner this project publishes is the
                                non-root 65532.

                                IT IS A PRIVILEGE GRANT AND IT IS SEPARATE FROM Privileged ON PURPOSE,
                                because the two are not the same thing and one does not imply the other.
                                Linux drops a process's effective capabilities when it switches away from
                                root, so a container that is `privileged: true` and runs as uid 65532 does
                                NOT hold CAP_SYSLOG — which is exactly what reading /dev/kmsg needs on any
                                host with kernel.dmesg_restrict=1. That combination looks sufficient, is
                                not, and produces a probe that silently reports nothing (issue #134).

                                Set it to 0 only for a test that genuinely needs a root-owned host
                                resource, and prefer the alternative where one exists: a kernel log the
                                image's own uid can read, named through the runner's environment, needs no
                                privilege at all. Every runner here degrades honestly without it — a source
                                it could not read is reported as unread, never as clean.
                              format: int64
                              type: integer
                          type: object
                        scope:
                          default: Node
                          description: Scope is the topology the test needs.
                          enum:
                          - Node
                          - Pair
                          - Group
                          type: string
                        soak:
                          description: |-
                            Soak divides this test's DurationSeconds into a sequence of shorter pod
                            executions. Unset means one pod for the whole duration, which is what
                            every test did before this field existed.
                          properties:
                            abortEarly:
                              default: false
                              description: |-
                                AbortEarly stops the soak as Failed the moment the aggregate so far
                                PROVABLY violates a gate. Default false.

                                "Provably" is the whole of it, and it is narrower than it sounds. It fires
                                only where the aggregation is monotone IN THE GATED DIRECTION, so that no
                                later segment could retract the violation: a Sum counter under
                                LessThanOrEqual (or under Equal, once the sum has passed the value), a Min
                                under a GreaterThanOrEqual floor, a Max under a LessThanOrEqual ceiling.
                                Everywhere else it stays silent — a Min under a ceiling can be pulled back
                                under it by a later segment, and aborting a week of burn-in on a violation
                                the next fifteen minutes would have retracted is the wrong trade.

                                It is evaluated at SEGMENT BOUNDARIES ONLY, from harvested metrics, and
                                never from a checkpoint's parse of a bounded log tail. A checkpoint is
                                evidence and never a verdict; ending a soak on a truncated read would make
                                it one.

                                A metric that has not been reported by any segment yet never aborts
                                anything. Fail-closed still applies at the end — a gated metric no segment
                                ever emitted fails the test — but absence mid-soak is not yet an absence.
                              type: boolean
                            segmentSeconds:
                              description: |-
                                SegmentSeconds is one pod's window. Each segment gets
                                BURNIN_DURATION_SECONDS=<segmentSeconds> and its own
                                activeDeadlineSeconds, so an eviction or a reboot costs one segment
                                rather than the whole run.

                                The floor is five minutes and it is a real bound rather than a round
                                number: a segment boundary costs a pod teardown, a pod creation and a
                                scheduling round-trip, and below five minutes the run spends more time
                                changing pods than it spends measuring hardware.

                                EVERY SEGMENT IS A FULL WINDOW, including the last, so a soak runs at
                                least its DurationSeconds and may overrun by up to one segment. A short
                                remainder segment would be the worse choice twice over: it is below the
                                floor this field exists to enforce, and a counter summed over a
                                ten-second window is not comparable to the same counter summed over a
                                fifteen-minute one, which is precisely what the aggregation rules assume.

                                It must not exceed DurationSeconds — a segment longer than the soak is a
                                soak that burns longer than it was asked to — and the run is refused at
                                start if it does.
                              format: int32
                              minimum: 300
                              type: integer
                          required:
                          - segmentSeconds
                          type: object
                        thresholds:
                          description: |-
                            Thresholds are pass/fail gates evaluated against parsed metrics
                            (e.g. busBandwidthGBs>=20, eccErrors==0, throttleEvents==0). A test with no
                            thresholds passes on a clean (exit 0) run.
                          items:
                            description: |-
                              Threshold gates a named metric emitted by the runner's result parser.

                              A threshold naming a metric the runner did not emit is a FAILURE, never a
                              pass: a missing measurement must not silently satisfy acceptance.

                              Choosing the comparison matters as much as choosing the number — see
                              Comparison. In short: dimensional metrics (a name ending in a unit suffix)
                              take GreaterThanOrEqual/LessThanOrEqual; dimensionless counters take Equal or
                              NotEqual, which are exact.
                            properties:
                              applicability:
                                default: Required
                                description: |-
                                  Applicability decides what happens when the hardware cannot produce this
                                  metric. Defaults to Required, which is today's fail-closed behaviour.
                                enum:
                                - Required
                                - RequiredIfMeasurable
                                type: string
                              comparison:
                                description: |-
                                  Comparison is the operator applied by a Threshold. Metric and value are both
                                  parsed as float64 and compared with NO TOLERANCE — there is no epsilon
                                  anywhere in this API, and that is what makes each operator right for exactly
                                  one kind of metric.

                                  GreaterThanOrEqual and LessThanOrEqual are how a MEASUREMENT is gated: a
                                  floor, a ceiling, or the two together for a band. Anything with a unit —
                                  bandwidth, temperature, latency, clocks, a percentage — belongs here.

                                  Equal and NotEqual are EXACT, and they exist for DIMENSIONLESS COUNTERS:
                                  eccErrors Equal 0, throttleEvents Equal 0, miscompares Equal 0. On a counter
                                  exactness is the whole point, because a tolerance around zero ECC errors is a
                                  tolerance for ECC errors.

                                  DO NOT use Equal or NotEqual on a continuous measurement.
                                  `sustainedClockPct Equal 83.22` asks an averaged sample to reproduce a decimal
                                  string exactly; it will not, so the gate fails on every healthy node forever
                                  and each failure is reported in the same shape as a hardware verdict. A metric
                                  whose name ends in a unit suffix (Gbps, GBs, MBs, Us, Ms, S, C, W, Pct, MHz,
                                  Tflops) is continuous by construction — gate it with GreaterThanOrEqual
                                  and/or LessThanOrEqual instead. pkg/verdict.ValidateThresholds reports this
                                  misuse at authoring time, before a run has condemned anything: the operator
                                  runs it against the pinned plan at run start and records what it found on the
                                  BurnInRun's ThresholdsSound condition.
                                enum:
                                - GreaterThanOrEqual
                                - LessThanOrEqual
                                - Equal
                                - NotEqual
                                type: string
                              metric:
                                description: |-
                                  Metric is the canonical metric name to gate, as the runner's result
                                  parser emits it — e.g. "busBandwidthGBs", "eccErrors", "latencyUs".
                                  Names are lowerCamelCase and a dimensional metric ends in a unit suffix;
                                  a name that breaks that grammar is dropped during parsing, so a threshold
                                  naming one can never be satisfied.

                                  The pattern is the apiserver's half of that rule and it is deliberately
                                  only the half a regex can express: lowerCamelCase, which is what
                                  contract.ValidateMetricName checks first. The other half — that a name
                                  this project OWNS declares the unit it is registered with — needs the
                                  registry and is enforced at plan time, where a violation refuses the run
                                  as a config Error rather than condemning a node.
                                pattern: ^[a-z][a-zA-Z0-9]*$
                                type: string
                              value:
                                description: |-
                                  Value is the number the metric is compared against, parsed as float64.
                                  It must be finite: "NaN" and "Inf" parse as numbers but express no bound,
                                  and NaN in particular compares false against everything, which would turn
                                  NotEqual into a gate that always passes.

                                  Under Equal/NotEqual the comparison is exact, so this should be a whole
                                  number counted by a counter. Under GreaterThanOrEqual/LessThanOrEqual it
                                  is a bound and any finite value is meaningful.

                                  The pattern admits exactly the finite decimal forms ("20", "10.8", "-3",
                                  "1e9") and refuses the ones that parse as float64 without expressing a
                                  bound ("NaN", "Inf", "+Inf"), along with the ones that do not parse at all
                                  ("twenty"). Every one of those is a gate that fails on every node forever
                                  and reports it in the shape of a hardware verdict, so the apiserver is the
                                  right place to say no — while the author is still holding the file.
                                pattern: ^[+-]?([0-9]+(\.[0-9]*)?|\.[0-9]+)([eE][+-]?[0-9]+)?$
                                type: string
                            required:
                            - comparison
                            - metric
                            - value
                            type: object
                          type: array
                      required:
                      - kind
                      type: object
                    required:
                      default: true
                      description: |-
                        Required marks whether this test's failure fails the whole profile
                        (default true). Informational tests can be marked false.
                      type: boolean
                    testRef:
                      description: TestRef names an existing BurnInTest.
                      type: string
                    variants:
                      description: |-
                        Variants expands this entry into one execution per cell of a matrix.

                        compute-smoke runs an NVFP4 GEMM. That is one cell of a matrix the API
                        had no word for: the same kernel at FP8, BF16, TF32 and FP64 answers
                        different questions about the same silicon, and asking all five meant
                        authoring five BurnInTest objects differing by one environment variable,
                        each with its own name, its own thresholds, and its own copy of every
                        other field. The same shape recurs on every axis worth sweeping — message
                        size for a fabric test, buffer size for a bandwidth test, duration class
                        for a soak.

                        Empty means one execution, exactly as before. A profile with no variants
                        plans identically to one written before variants existed.
                      items:
                        description: |-
                          TestVariant is one cell of a test's matrix.

                          # Why a list and not a cartesian product

                          A `matrix.axes` block that multiplied out was considered and rejected. It
                          needs exclusion lists for the cells that make no sense, a cell-naming scheme,
                          and per-cell thresholds anyway — at which point it has collapsed back into
                          this list with extra machinery in front of it. Variants are the primitive;
                          cartesian expansion can be layered on later as client-side sugar that emits
                          them.

                          # Why the controller never reads Axes

                          Axes are OPAQUE labels. The controller injects them and echoes them and does
                          not interpret them, ever. That is what keeps the vendor seam where it belongs:
                          a precision, a message size or a duration class means something to a runner
                          and nothing to a reconciler, and a controller that learned to branch on
                          `precision: fp4` would be the `if nvidia {}` this project refuses, wearing a
                          different hat.
                        properties:
                          args:
                            items:
                              type: string
                            type: array
                          axes:
                            additionalProperties:
                              type: string
                            description: |-
                              Axes describe what varies, as opaque labels: {precision: fp4},
                              {class: soak}, {messageBytes: "8"}.

                              Each becomes BURNIN_VARIANT_<AXIS>=<value> in the runner's environment,
                              upper-cased, and is echoed onto the TestResult and into the delivery
                              envelope so a report can group a sweep's cells without parsing them back
                              out of names.
                            type: object
                          durationSeconds:
                            description: |-
                              The overlays. Nil INHERITS the parent test's value; non-nil REPLACES it.

                              Thresholds replace rather than merge, and that is the one that matters.
                              An FP4 throughput floor is not an edited FP8 floor. Merging by metric name
                              would silently retain a gate the author believed they had replaced — and a
                              node failed against a threshold nobody can find in the profile is the
                              worst kind of verdict this project can produce, because there is nothing
                              to read that explains it.

                              A variant that means to apply NO thresholds sets an empty (non-nil) list.
                            format: int32
                            type: integer
                          env:
                            items:
                              description: EnvVar represents an environment variable
                                present in a Container.
                              properties:
                                name:
                                  description: |-
                                    Name of the environment variable.
                                    May consist of any printable ASCII characters except '='.
                                  type: string
                                value:
                                  description: |-
                                    Variable references $(VAR_NAME) are expanded
                                    using the previously defined environment variables in the container and
                                    any service environment variables. If a variable cannot be resolved,
                                    the reference in the input string will be unchanged. Double $$ are reduced
                                    to a single $, which allows for escaping the $(VAR_NAME) syntax: i.e.
                                    "$$(VAR_NAME)" will produce the string literal "$(VAR_NAME)".
                                    Escaped references will never be expanded, regardless of whether the variable
                                    exists or not.
                                    Defaults to "".
                                  type: string
                                valueFrom:
                                  description: Source for the environment variable's
                                    value. Cannot be used if value is not empty.
                                  properties:
                                    configMapKeyRef:
                                      description: Selects a key of a ConfigMap.
                                      properties:
                                        key:
                                          description: The key to select.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the ConfigMap
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fieldRef:
                                      description: |-
                                        Selects a field of the pod: supports metadata.name, metadata.namespace, `metadata.labels['<KEY>']`, `metadata.annotations['<KEY>']`,
                                        spec.nodeName, spec.serviceAccountName, status.hostIP, status.podIP, status.podIPs.
                                      properties:
                                        apiVersion:
                                          description: Version of the schema the FieldPath
                                            is written in terms of, defaults to "v1".
                                          type: string
                                        fieldPath:
                                          description: Path of the field to select
                                            in the specified API version.
                                          type: string
                                      required:
                                      - fieldPath
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    fileKeyRef:
                                      description: |-
                                        FileKeyRef selects a key of the env file.
                                        Requires the EnvFiles feature gate to be enabled.
                                      properties:
                                        key:
                                          description: |-
                                            The key within the env file. An invalid key will prevent the pod from starting.
                                            The keys defined within a source may consist of any printable ASCII characters except '='.
                                            During Alpha stage of the EnvFiles feature gate, the key size is limited to 128 characters.
                                          type: string
                                        optional:
                                          default: false
                                          description: |-
                                            Specify whether the file or its key must be defined. If the file or key
                                            does not exist, then the env var is not published.
                                            If optional is set to true and the specified key does not exist,
                                            the environment variable will not be set in the Pod's containers.

                                            If optional is set to false and the specified key does not exist,
                                            an error will be returned during Pod creation.
                                          type: boolean
                                        path:
                                          description: |-
                                            The path within the volume from which to select the file.
                                            Must be relative and may not contain the '..' path or start with '..'.
                                          type: string
                                        volumeName:
                                          description: The name of the volume mount
                                            containing the env file.
                                          type: string
                                      required:
                                      - key
                                      - path
                                      - volumeName
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    resourceFieldRef:
                                      description: |-
                                        Selects a resource of the container: only resources limits and requests
                                        (limits.cpu, limits.memory, limits.ephemeral-storage, requests.cpu, requests.memory and requests.ephemeral-storage) are currently supported.
                                      properties:
                                        containerName:
                                          description: 'Container name: required for
                                            volumes, optional for env vars'
                                          type: string
                                        divisor:
                                          anyOf:
                                          - type: integer
                                          - type: string
                                          description: Specifies the output format
                                            of the exposed resources, defaults to
                                            "1"
                                          pattern: ^(\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))(([KMGTPE]i)|[numkMGTPE]|([eE](\+|-)?(([0-9]+(\.[0-9]*)?)|(\.[0-9]+))))?$
                                          x-kubernetes-int-or-string: true
                                        resource:
                                          description: 'Required: resource to select'
                                          type: string
                                      required:
                                      - resource
                                      type: object
                                      x-kubernetes-map-type: atomic
                                    secretKeyRef:
                                      description: Selects a key of a secret in the
                                        pod's namespace
                                      properties:
                                        key:
                                          description: The key of the secret to select
                                            from.  Must be a valid secret key.
                                          type: string
                                        name:
                                          default: ""
                                          description: |-
                                            Name of the referent.
                                            This field is effectively required, but due to backwards compatibility is
                                            allowed to be empty. Instances of this type with an empty value here are
                                            almost certainly wrong.
                                            More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                                          type: string
                                        optional:
                                          description: Specify whether the Secret
                                            or its key must be defined
                                          type: boolean
                                      required:
                                      - key
                                      type: object
                                      x-kubernetes-map-type: atomic
                                  type: object
                              required:
                              - name
                              type: object
                            type: array
                          name:
                            description: |-
                              Name suffixes the parent test's name and is this variant's RESULT
                              IDENTITY: the execution is planned as "<test>-<name>".

                              Two variants of one test with the same name are refused at plan time by
                              the same unique-name check that refuses two tests with the same name,
                              because the failure is identical — the second would never run and its
                              verdict would silently overwrite the first's.
                            minLength: 1
                            pattern: ^[a-z0-9]([-a-z0-9]*[a-z0-9])?$
                            type: string
                          repeatCount:
                            format: int32
                            minimum: 1
                            type: integer
                          thresholds:
                            items:
                              description: |-
                                Threshold gates a named metric emitted by the runner's result parser.

                                A threshold naming a metric the runner did not emit is a FAILURE, never a
                                pass: a missing measurement must not silently satisfy acceptance.

                                Choosing the comparison matters as much as choosing the number — see
                                Comparison. In short: dimensional metrics (a name ending in a unit suffix)
                                take GreaterThanOrEqual/LessThanOrEqual; dimensionless counters take Equal or
                                NotEqual, which are exact.
                              properties:
                                applicability:
                                  default: Required
                                  description: |-
                                    Applicability decides what happens when the hardware cannot produce this
                                    metric. Defaults to Required, which is today's fail-closed behaviour.
                                  enum:
                                  - Required
                                  - RequiredIfMeasurable
                                  type: string
                                comparison:
                                  description: |-
                                    Comparison is the operator applied by a Threshold. Metric and value are both
                                    parsed as float64 and compared with NO TOLERANCE — there is no epsilon
                                    anywhere in this API, and that is what makes each operator right for exactly
                                    one kind of metric.

                                    GreaterThanOrEqual and LessThanOrEqual are how a MEASUREMENT is gated: a
                                    floor, a ceiling, or the two together for a band. Anything with a unit —
                                    bandwidth, temperature, latency, clocks, a percentage — belongs here.

                                    Equal and NotEqual are EXACT, and they exist for DIMENSIONLESS COUNTERS:
                                    eccErrors Equal 0, throttleEvents Equal 0, miscompares Equal 0. On a counter
                                    exactness is the whole point, because a tolerance around zero ECC errors is a
                                    tolerance for ECC errors.

                                    DO NOT use Equal or NotEqual on a continuous measurement.
                                    `sustainedClockPct Equal 83.22` asks an averaged sample to reproduce a decimal
                                    string exactly; it will not, so the gate fails on every healthy node forever
                                    and each failure is reported in the same shape as a hardware verdict. A metric
                                    whose name ends in a unit suffix (Gbps, GBs, MBs, Us, Ms, S, C, W, Pct, MHz,
                                    Tflops) is continuous by construction — gate it with GreaterThanOrEqual
                                    and/or LessThanOrEqual instead. pkg/verdict.ValidateThresholds reports this
                                    misuse at authoring time, before a run has condemned anything: the operator
                                    runs it against the pinned plan at run start and records what it found on the
                                    BurnInRun's ThresholdsSound condition.
                                  enum:
                                  - GreaterThanOrEqual
                                  - LessThanOrEqual
                                  - Equal
                                  - NotEqual
                                  type: string
                                metric:
                                  description: |-
                                    Metric is the canonical metric name to gate, as the runner's result
                                    parser emits it — e.g. "busBandwidthGBs", "eccErrors", "latencyUs".
                                    Names are lowerCamelCase and a dimensional metric ends in a unit suffix;
                                    a name that breaks that grammar is dropped during parsing, so a threshold
                                    naming one can never be satisfied.

                                    The pattern is the apiserver's half of that rule and it is deliberately
                                    only the half a regex can express: lowerCamelCase, which is what
                                    contract.ValidateMetricName checks first. The other half — that a name
                                    this project OWNS declares the unit it is registered with — needs the
                                    registry and is enforced at plan time, where a violation refuses the run
                                    as a config Error rather than condemning a node.
                                  pattern: ^[a-z][a-zA-Z0-9]*$
                                  type: string
                                value:
                                  description: |-
                                    Value is the number the metric is compared against, parsed as float64.
                                    It must be finite: "NaN" and "Inf" parse as numbers but express no bound,
                                    and NaN in particular compares false against everything, which would turn
                                    NotEqual into a gate that always passes.

                                    Under Equal/NotEqual the comparison is exact, so this should be a whole
                                    number counted by a counter. Under GreaterThanOrEqual/LessThanOrEqual it
                                    is a bound and any finite value is meaningful.

                                    The pattern admits exactly the finite decimal forms ("20", "10.8", "-3",
                                    "1e9") and refuses the ones that parse as float64 without expressing a
                                    bound ("NaN", "Inf", "+Inf"), along with the ones that do not parse at all
                                    ("twenty"). Every one of those is a gate that fails on every node forever
                                    and reports it in the shape of a hardware verdict, so the apiserver is the
                                    right place to say no — while the author is still holding the file.
                                  pattern: ^[+-]?([0-9]+(\.[0-9]*)?|\.[0-9]+)([eE][+-]?[0-9]+)?$
                                  type: string
                              required:
                              - comparison
                              - metric
                              - value
                              type: object
                            type: array
                        required:
                        - name
                        type: object
                      type: array
                  type: object
                minItems: 1
                type: array
            required:
            - tests
            type: object
          status:
            properties:
              observedGeneration:
                format: int64
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
