---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.2
  name: burninsinks.burnin.glimmer.ai
spec:
  group: burnin.glimmer.ai
  names:
    categories:
    - burnin
    kind: BurnInSink
    listKind: BurnInSinkList
    plural: burninsinks
    shortNames:
    - bis
    singular: burninsink
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: |-
          BurnInSink is a result-export target — the generic contract that lets an
          external control plane consume burn-in verdicts with no code dependency.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: BurnInSinkSpec configures a result export target.
            properties:
              configMap:
                description: ConfigMap config (Type=ConfigMap).
                properties:
                  name:
                    type: string
                required:
                - name
                type: object
              type:
                description: |-
                  SinkType is where a BurnInRun's verdict is exported.

                  This is the ONLY integration seam with an external control plane. The operator
                  never imports Glimmer (or any consumer); it POSTs a generic result document to
                  a webhook, so Glimmer — or Prometheus, or a customer's own system — consumes
                  burn-in verdicts without a code dependency. That standalone contract is what
                  makes this repo publishable on its own.
                enum:
                - Webhook
                - Prometheus
                - ConfigMap
                type: string
              webhook:
                description: Webhook config (Type=Webhook).
                properties:
                  insecureSkipVerify:
                    description: InsecureSkipVerify disables TLS verification (dev
                      only).
                    type: boolean
                  tokenSecretRef:
                    description: TokenSecretRef references a Secret key holding a
                      bearer token (optional).
                    properties:
                      key:
                        description: The key of the secret to select from.  Must be
                          a valid secret key.
                        type: string
                      name:
                        default: ""
                        description: |-
                          Name of the referent.
                          This field is effectively required, but due to backwards compatibility is
                          allowed to be empty. Instances of this type with an empty value here are
                          almost certainly wrong.
                          More info: https://kubernetes.io/docs/concepts/overview/working-with-objects/names/#names
                        type: string
                      optional:
                        description: Specify whether the Secret or its key must be
                          defined
                        type: boolean
                    required:
                    - key
                    type: object
                    x-kubernetes-map-type: atomic
                  url:
                    type: string
                required:
                - url
                type: object
            required:
            - type
            type: object
          status:
            properties:
              lastDelivery:
                format: date-time
                type: string
              lastError:
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
